[project]
name = "raviolichess"
description = "chess server full-stack application"
readme = "README.md"
version = "0.1.0"
requires-python = ">=3.13"
[dependency-groups]
dev = [
    "honcho",
    "ruff>=0.14.10",
]
[tool.uv.workspace]
members = ["backend/*"]
exclude = ["backend/scripts"]

[tool.uv.sources]
ravioli-core = { workspace = true }