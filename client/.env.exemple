# Choose either the Docker or default configuration below,
# then copy your chosen configuration into a new file named `.env.development`(default) or `.env.docker`.

#
# Default configuration
#

BASE='/static/web/'
BACKEND_URL='http://127.0.0.1:8000'

#
# Docker configuration
#

BASE='/static/web/'
BACKEND_URL='http://backend:8000'
